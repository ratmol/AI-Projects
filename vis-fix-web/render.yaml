services:
  - type: web
    name: vis-fix
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OPENROUTER_API_KEY
        sync: false # set in the Render dashboard, never in git
      - key: TAVILY_API_KEY
        sync: false
